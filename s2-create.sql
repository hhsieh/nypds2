create table if not exists s2 (
  programID text,
  season text,
  composerName text,
  workTitle text,
  movement text,
  conductorName text,
  soloistName text,
  soloistInstrument text,
  soloistRole text
);

